name: CI - Build and Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2
    
    - name: Create environment files (if they don't exist)
      run: |
        touch server/.env
        touch client/.env
    
    - name: Build Backend Docker image
      run: |
        cd server
        docker build -t auth-backend:latest .
      
    - name: Build Frontend Docker image
      run: |
        cd client
        docker build -t auth-frontend:latest .
    
    - name: Start services with Docker Compose
      run: |
        docker compose up -d --build
    
    - name: Wait for services to be ready
      run: sleep 15
    
    - name: Check running containers
      run: docker compose ps
    
    - name: Test backend health (optional)
      run: |
        curl -f http://localhost:5000/health || echo "Backend health check skipped"
      continue-on-error: true
    
    - name: Show logs if failed
      if: failure()
      run: |
        echo "=== Backend Logs ==="
        docker compose logs backend
        echo "=== Frontend Logs ==="
        docker compose logs frontend
    
    - name: Cleanup
      if: always()
<<<<<<< HEAD
      run: docker compose down -v
=======
      run: docker compose down -v
>>>>>>> 25e8544 (Add complete Docker setup with CI/CD pipelines)
