"use client";

import React from "react";

export const ContentShortcuts: React.FC = () => {
  return (
    <div className="w-full">
      {/* This component is now empty as the trio buttons have been removed */}
      {/* The dashboard will display RecentLessons in full width */}
    </div>
  );
};
